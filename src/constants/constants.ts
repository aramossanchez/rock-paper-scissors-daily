import type { ArrowClasses } from "../types/types";

export const ARROW_CLASSES: ArrowClasses[] = [
  "arrow-top-left",
  "arrow-top-right",
  "arrow-bottom-left",
  "arrow-bottom-right",
] as const;
